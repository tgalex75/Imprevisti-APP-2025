# Ignora i volumi di Supabase (causa principale dell'errore e inutili per il build)
supabase-project/

# Ignora altre cartelle/file comuni non necessari per il build dell'immagine
.git
node_modules
build
dist
.env
*.env
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Potresti anche voler ignorare l'intera cartella supabase se non serve nulla
# al build del frontend (valuta attentamente se il Dockerfile fa riferimento
# a qualche file al suo interno, anche se di solito non dovrebbe).
# Se sei sicuro, puoi sostituire la prima riga con questa:
# supabase-project/
